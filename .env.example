# SMTP Configuration for Contact Form
# Replace with your actual email service details (e.g. Google Workspace, Gmail App Password, Zoho, Hostinger, cPanel, AWS SES, etc.)

SMTP_HOST="smtp.gmail.com"
SMTP_PORT=465
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"

# Destination inbox where the CEO receives incoming portfolio inquiries
CONTACT_RECEIVER_EMAIL="faizal@adamgroup.ae"
